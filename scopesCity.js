let city = 'Boston'

if (true) {     // Simplest predicate statement
    let city = 'Woooooooostah'
    //console.log(city) // Will only work inside block
}
console.log(city) // Will run only outside variable if console.log inside block is not running